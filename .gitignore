# Dependencies
node_modules/
_corrupted_node_modules_stubs/
_corrupted*/
.pnp
.pnp.js

# Build
dist/
build/
.next/
out/
.strapi/
.tmp/

# Environment (never commit secrets)
.env
.env.local
.env.*.local
!.env.example
# Local-only env files carrying real credentials — never commit
.env.gitlab
.env.gitlab.*

# Logs
logs/
*.log
npm-debug.log*

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
.cursor/
.anima/
.kilocode/
.projectmanager/
.vs/

# Docker
docker/data/

# Strapi
apps/cms/.cache/
apps/cms/public/uploads/

# Testing
coverage/
test-results/
playwright-report/
e2e/*-snapshots/

# Internal docs (not for public repo)
competitor-research-progress.md

# Misc
*.tsbuildinfo
.turbo/
server.log
*.err

# Temporary scripts
tmp_*.py
temp_*.py
temp_*.sh

# Files containing secrets — NEVER commit
*.secret
*.secrets

# Stale build/export artifacts
--output
project.zip
standalone
apps/frontend/app/

# TypeScript build outputs (packages/* compiled JS/d.ts)
packages/*/*.d.ts
packages/*/*.js
packages/*/*.js.map

# Backend build output (not committed)
apps/backend/dist/

# LLM chat history
.llm-chat-history/

# AI tool configs (not project code)
.claude/
.kilo/
.memory/
.qoder/
.trae/
paseo-hooks/
.gitwandrc
opencode.json
.agents/
.codex/
.gemini/
.junie/

# Temporary output files
.output.txt

# Python (AI agent scripts)
.venv/
requirements.txt

# Netlify build cache
apps/frontend/.netlify/

# Lighthouse report
lh-report.json

# Deploy scripts (keep local — contain server connection logic)
deploy.py
sshrun.py
upload_script*.py

# Lighthouse CI temp data
.lighthouse-tmp/
HEXA-Vision-Playbook/**/lighthouse-*.json

# Large 3D source assets (not web-ready)
*.max
*.rar
*.zip
*.tar.gz
*.7z
*.fbx
*.obj
apps/frontend/public/models/.gitkeep

.vercel
.vercel-tmp/
# Note: .env.example is explicitly allowed above (line 20). All other .env* patterns
# are already covered by .env, .env.local, .env.*.local rules on lines 17-19.
pnpm-workspace.yaml

# Stray deployment scratch (web-root copies, typo dirs, deploy logs)
var/
hexa-hab/
deploy_log.txt

# Archived operational artifacts (dead scripts, logs, bundles, one-off fixes)
# Root should hold only manifests — see ops/ for the archive inventory.
ops/archive/
docker.tar.gz

# SSH private keys (never commit)
*.key
hexastudio_key
