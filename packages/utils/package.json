{
  "name": "@hexastudio/utils",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "shiki": "^4.4.1"
  }
}
