{
  "name": "@hexastudio/types",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"*.ts\" \"**/*.ts\" --ext .ts --max-warnings=0 --ignore-pattern dist/"
  },
  "dependencies": {
    "shiki": "^4.4.1"
  }
}
