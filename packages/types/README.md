# @hexastudio/types

This package contains shared TypeScript types used across HexaStudio frontend, backend, and CMS.

Usage:

In other workspaces (example):

```ts
import type { Project, SceneManifest } from '@hexastudio/types';
```
