import React from 'react';import { cn } from '@/lib/utils';interface ModalProps {  isOpen: boolean;  onClose: () => void;  title?: string;  children: React.ReactNode;  className?: string;}export const Modal = ({ isOpen, onClose, title, children, className }: ModalProps) => {  if (!isOpen) return null;  return (    <div className="fixed inset-0 z-50 flex items-center justify-center p-4">            <div         className="absolute inset-0 bg-black/60 backdrop-blur-sm"         onClick={onClose}      />                  <div className={cn(        'relative w-full max-w-lg overflow-hidden bg-surface border border-border p-8 transition-all duration-300',        className      )}>        {title && (          <h2 className="mb-6 text-2xl font-light tracking-tight text-foreground">            {title}          </h2>        )}        <div className="relative z-10">          {children}        </div>        <button           onClick={onClose}          className="absolute top-4 right-4 text-neutral-500 hover:text-foreground transition-colors"        >          ✕        </button>      </div>    </div>  );};