'use client';import React from 'react';import Link from 'next/link';import { ScrollFadeIn } from '@/components/ScrollFadeIn';import { Button } from '@/components/ui/Button';import { motion } from 'framer-motion';export function CTASection() {  return (    <section className="relative px-8 md:px-16 py-48 overflow-hidden bg-surface border-t border-border/50">            <div className="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full bg-[radial-gradient(circle_at_center,var(--color-accent)/0.05_0%,transparent_70%)] pointer-events-none" />            <ScrollFadeIn className="max-w-4xl mx-auto text-center relative z-10">        <motion.span           className="text-[11px] uppercase tracking-[0.5em] text-neutral-500 mb-8 block"        >          Collaboration        </motion.span>        <motion.h2           className="text-5xl md:text-8xl font-serif font-light tracking-tighter text-foreground mb-12 leading-[1.1]"        >          Ready to Define Your <br />          <span className="italic text-accent">Next Space?</span>        </motion.h2>        <motion.p           className="text-lg text-neutral-400 font-light leading-relaxed mb-16 max-w-xl mx-auto"        >          Every great project begins with a conversation. Reach out and let's explore          what we can create together.        </motion.p>        <Link href="/contact">          <Button variant="primary" size="lg" className="group">            Get in Touch            <span className="ml-3 transition-transform duration-300 group-hover:translate-x-1">→</span>          </Button>        </Link>      </ScrollFadeIn>                  <div className="absolute bottom-0 left-0 w-px h-32 bg-gradient-to-t from-accent to-transparent opacity-20" />      <div className="absolute bottom-0 right-0 w-px h-32 bg-gradient-to-t from-accent to-transparent opacity-20" />    </section>  );}