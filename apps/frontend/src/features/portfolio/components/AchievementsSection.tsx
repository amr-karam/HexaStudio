'use client';import React from 'react';import { motion } from 'framer-motion';const stats = [  { value: '12+', label: 'Countries Served' },  { value: '200+', label: 'Projects Delivered' },  { value: '8', label: 'Years Excellence' },  { value: '100%', label: 'Client Satisfaction' },];export const AchievementsSection = () => {  return (    <section className="px-8 md:px-16 py-24 bg-background">      <div className="max-w-screen-2xl mx-auto">        <div className="grid grid-cols-2 md:grid-cols-4 gap-12 md:gap-24">          {stats.map((stat, idx) => (            <motion.div              key={stat.label}              initial={{ opacity: 0, y: 20 }}              whileInView={{ opacity: 1, y: 0 }}              viewport={{ once: true }}              transition={{ duration: 0.8, delay: idx * 0.1, ease: [0.16, 1, 0.3, 1] }}              className="text-center group"            >              <span className="text-5xl md:text-6xl font-serif text-accent font-light block mb-4 transition-transform duration-500 group-hover:scale-110">                {stat.value}              </span>              <span className="text-[11px] uppercase tracking-[0.4em] text-neutral-500 group-hover:text-neutral-300 transition-colors duration-500">                {stat.label}              </span>            </motion.div>          ))}        </div>      </div>    </section>  );};