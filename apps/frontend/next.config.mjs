// Re-export the Next.js config from next.config.ts.
// This ESM module ensures compatibility with tools that expect
// a next.config.mjs entry point alongside the TypeScript config.
export { default } from "./next.config.ts";
